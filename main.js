//Declaraão/criação da função tocaSomPom

function tocaSom(ideElementoAudio) (

    document.querySelector(ideElementoAudio).play();
    //criando a referencias constante listaDeTeclas e buscando a classe coletiva tecl const listaDeTeclas = document.querySelectorAll('.tecla'); 
     //ceiação da repetição "while" e trocado pelo "for" com variavel, condição e increm for(let contador = 0; contador < listaDeTeclas.length; contador++) {
        const teclas listaDeTeclas [contador]; //guarda o contador da lista const efeito teclas.classList[1]; //chama o 2 item classe individual do som const idAudio #som Sefeito); //utiliza a junção template string ${}

tocasom(idAudio):

teclas.onclick = function(){ //função anônima auxiliar para chamar tocasom //chamada da função tocasom com parâmetro idAudio

teclas.onkeydown = function(evento) {

console.log(evento.code == 'Space')

if (evento.code == 'Space')( if(evento.code != 'Tab'){ teclas.classList.add('ativa');

teclas.onkeyup=function(){ teclas.classList.remove('ativa');